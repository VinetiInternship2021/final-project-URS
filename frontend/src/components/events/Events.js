import React from 'react';
import EventRooms from './EventRooms';

const Events = function () {
    return (
        <React.Fragment>
            <EventRooms/>
        </React.Fragment>
    );
};

export default Events;
